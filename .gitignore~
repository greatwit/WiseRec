Mcndk/obj		
RealTest/obj		# 忽略 h264/ 目录下的所有文件
/EventServer/build		# 忽略 build/ 目录下的所有文件
/EventClient/*.o		#忽略EventClient所有以 .o 或 .a 结尾的文件
/EventServer/*.o		#忽略EventServer所有以 .o 或 .a 结尾的文件
/EventClient/*~			#忽略~的所有文件
/EventServer/*~			#忽略~的所有文件
